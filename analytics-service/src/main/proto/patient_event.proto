syntax = "proto3";

// all generated code based on our proto file, will got to below package
package patient.events;

option java_multiple_files = true;

message PatientEvent {
  string patientId = 1;
  string name = 2;
  string email = 3;
  string event_type = 4;
}

